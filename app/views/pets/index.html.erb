<h1>All my Pets</h1>

<ul>
  <% @pets.each do |pet| %>
    <li><%= link_to pet.name, pet_path(pet) %>, found <%= pet.found_days_ago %> days ago</li>
  <% end %>
</ul>

<%= link_to "Found a pet!", new_pet_path %>
