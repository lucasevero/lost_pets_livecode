<h1><%= @pet.name %></h1>
<h2><%= @pet.address %></h2>
<p><%= @pet.species %> | Found <%= @pet.found_days_ago %> days ago</p>

<%= link_to "Back to pets", pets_path %>
<%= link_to "Edit this pet", edit_pet_path(@pet) %>
<%= link_to "Delete this pet", pet_path(@pet), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
